[pytest]
testpaths =
    tests
    _source/Lab
pythonpath =
    .
    _source
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    chaos: adversarial failure-injection suite (Phase 3.1; run nightly/pre-gate)
    property: hypothesis-based invariant tests
    integration: multi-component wiring tests
addopts = -ra
